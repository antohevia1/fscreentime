STAGE=dev

# OAuth — get these from Google Cloud Console and Facebook Developer Portal
GOOGLE_CLIENT_ID=REPLACE_ME
GOOGLE_CLIENT_SECRET=REPLACE_ME
FACEBOOK_APP_ID=REPLACE_ME
FACEBOOK_APP_SECRET=REPLACE_ME

# Cognito hosted UI domain suffix (must be globally unique)
COGNITO_DOMAIN_SUFFIX=your-unique-suffix

# Device ingest API key (generate a random string)
INGEST_API_KEY=REPLACE_ME

# OAuth callback URLs (update for production)
CALLBACK_URL=https://fscreentime.app/app
LOGOUT_URL=https://fscreentime.app/